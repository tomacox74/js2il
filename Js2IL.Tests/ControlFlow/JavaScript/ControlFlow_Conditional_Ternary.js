"use strict";

// Ternary operator demo
// Expected: prints "lte" then "lte"
const a = 3;
const b = 5;
const s1 = (a < b) ? "lte" : "gt";
const s2 = (a > b) ? "gt" : "lte";
console.log(s1);
console.log(s2);
