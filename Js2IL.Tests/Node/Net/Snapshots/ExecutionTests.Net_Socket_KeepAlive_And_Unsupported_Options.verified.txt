server keepalive same:true
client keepalive same:true
client nodelay same:true
client keepalive delay:NetSocket.setKeepAlive currently supports enable/disable only; initialDelay is not implemented.
client data:ok
server closed
