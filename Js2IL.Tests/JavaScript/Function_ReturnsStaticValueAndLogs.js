// Define a function that returns a static value
function getStaticValue() {
	return 1234;
}

// Call the function, capture the return, and log it
var value = getStaticValue();
console.log("returned value is", value);
