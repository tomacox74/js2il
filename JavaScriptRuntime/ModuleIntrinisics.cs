namespace JavaScriptRuntime
{
    /// <summary>
    /// Holds CommonJS module-scoped intrinsics (require, module, exports, __dirname, __filename).
    /// These bindings are per-module and shadow globalThis properties when resolved in module scope.
    /// </summary>
    public static class ModuleIntrinisics
    {
        // Placeholder for future module intrinsic resolution
        // Will contain registry and resolution logic for:
        // - require(id)
        // - module.exports
        // - exports
        // - __dirname (module-scoped)
        // - __filename (module-scoped)
    }
}
